import {CartIcon} from '../icons'
import { useSelector } from 'react-redux'

const Navbar = () =>{
    return (
        <nav>
<div className="nav-center">
    <h3>redux too</h3>
</div>
        </nav>
    )
}